# .NET build output
bin/
obj/
*.nupkg

# Windows
Thumbs.db
Desktop.ini

# Editor / temp
*.tmp
*~
.vs/

# The session index cache lives in ~/.claude, not here, but ignore it anyway
# in case anyone runs the tool from inside the repo with a relative path.
ccp-cache.json
